class Expenses {
  int? id;
  String? title;
  double? amount;
  DateTime? dateTime;

  Expenses(
      {required this.id,
      required this.amount,
      required this.title,
      required this.dateTime});
}
